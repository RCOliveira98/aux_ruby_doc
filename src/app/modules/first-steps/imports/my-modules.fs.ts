import { FontAwesomeModule } from '@fortawesome/angular-fontawesome';
import { SharedModule } from '../../shared/shared.module';

export const myModulesFs = [
    FontAwesomeModule,
    SharedModule
]